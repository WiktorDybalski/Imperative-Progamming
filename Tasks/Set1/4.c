/*4. void multiply by scalar(double v[], int n, double s);
Funkcja mnoży każdy element tablicy rzeczywistej v (o długości n) przez
liczbę rzeczywistą s. Wynik mnożenia jest zapisywany w tablicy v.*/

#include <stdio.h>


void multiply by scalar(double v[], int n, double s) {
    for(int i = 0; i < n; i++) {
        v[i] *= s;
    }
}

int main() {
    printf("siemano!");
    return 0;
}